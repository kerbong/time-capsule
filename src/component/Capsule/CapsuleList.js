import React from "react";

const CapsuleList = (props) => {
  return (
    <div>
      캡슐 리스트
      {/* 캡슐 두 개 보여주고, 열리기 까지 남은 시간 보여주기 */}
      {/* 내가 안 쓴 쪽찌가 있으면 css로 구분. 쓴건 회색, 안쓴건 컬러풀.. */}
      {/* 총 2개의 캡슐이 보이고 그 아래에 내가 썼는지(나, 우리에게) 가 보임.*/}
      {/**/}
    </div>
  );
};

export default CapsuleList;
